#include "holberton.h"
/**
 *mul - multiplier
 *@a: first variable
 *@b: second variable
 *Return: return always the result
 */

int mul(int a, int b)
{
int product;

	product = a * b;
return (product);
}
